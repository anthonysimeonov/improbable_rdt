package rrp_lcm;

// This is used to create a variable size array of doubles
struct joint_trajectory_with_gripper_t {
    // Number of elements in trajectory
    int32_t n_traj_length;
    int32_t n_dim_with_gripper;
    int32_t size;

    // Trajectory
    double joint_trajectory_with_gripper[size];
}
